<h1>All Bookings</h1>
<ul class="list-group">
  <% @bookings.each do |booking| %>
    <li class="list-group-item">
      <%= link_to booking.name, booking_path(booking) %>
    </li>
  <% end %>
  <%= link_to 'Book a DJ', new_booking_path %>
</ul>
